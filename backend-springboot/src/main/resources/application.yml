server:
  port: 8080

spring:
  application:
    name: videoguard-backend
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 220MB

videoguard:
  uploads-dir: uploads
  ai-service:
    base-url: http://localhost:8000

